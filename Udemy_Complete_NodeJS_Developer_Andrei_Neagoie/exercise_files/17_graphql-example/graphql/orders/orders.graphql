# ROOT LEVEL TYPES
type Query {
   orders: [Order]
}

# CUSTOM TYPES
type Order {
   date: String!
   subtotal: Float!
   items: [OrderItem]
}

type OrderItem {
   product: Product!
   quantity: Int!
}
